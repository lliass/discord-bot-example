import { ControllerConfiguration } from '../../../common/interfaces/controller.interface';

const monitorControllerConfig: ControllerConfiguration = {
  baseEndPoint: 'monitor',
};

export default monitorControllerConfig;
